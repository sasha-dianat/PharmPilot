"""هوش‌یار دارو — web-research engine that proposes SUGGESTED drug enrichments.

Nothing here writes to the deterministic ingest/link paths: the researcher only
produces status='suggested' rows for owner review. Approval (elsewhere) is what
promotes a row to 'approved' and lets it self-apply.
"""
